# fly.toml - configuration for Fly.io
app = "events-mavou"  # change to your app name, lowercase, no spaces
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[env]
APP_ENV = "production"
APP_DEBUG = "false"
APP_URL = "https://mavou-consulting-projects.fly.dev"
DB_CONNECTION = "mysql"
DB_HOST="sql8.freesqldatabase.com"
DB_PORT = "3306"
DB_DATABASE="sql8800720"
DB_USERNAME="sql8800720"
DB_PASSWORD="ZPLGRQlvet"

[build]
  # Use Dockerfile in project root
  dockerfile = "Dockerfile"
